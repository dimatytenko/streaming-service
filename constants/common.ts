export enum TimeFrame {
  DAY = 'day',
  WEEK = 'week',
}

export enum ActorsPathes {
  ACTORS = '/actors',
  TODAY = '/actors/today',
  WEEK = '/actors/week',
}

export enum CardVariants {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
}
